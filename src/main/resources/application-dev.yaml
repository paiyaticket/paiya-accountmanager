server:
  port: 8008
  servlet:
    context-path: /api

# Database
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: paiya_accountmanager
      username: ${PAIYA_DB_USERNAME:paiya}
      password: ${PAIYA_DB_PASSWORD:paiya}
